fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2

# Audio I/O + DSP — wheel-only stack.
# Avoids the librosa → numba → llvmlite chain that fails to compile
# on macOS when no matching LLVM wheel exists for your Python version.
numpy>=1.26
scipy>=1.11
soundfile>=0.12        # libsndfile bundled wheels: wav / flac / ogg / aiff
audioread>=3.0         # pure Python; uses macOS CoreAudio for mp3 / m4a / aac
