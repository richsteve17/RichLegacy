# Optional: when testing on iPhone over LAN, set this to your Mac's IP+port
# so the frontend hits the backend directly (the Vite proxy only works for
# requests that come through Vite itself).
# VITE_API_BASE=http://192.168.1.10:8000
